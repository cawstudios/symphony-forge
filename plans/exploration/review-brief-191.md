# Review brief — symphony-forge #191 task-level parallelism

Scope: feat/task-parallelism vs main (after #189). Design source: the brief the builder followed (dependency-gated task/stage start; a second active stage only when write scopes are area-disjoint across all worktrees of the story; per-task stage records under .factory/stories/<key>/stages/<task>.json with legacy single-file migration; forge next listing every ready task and active stages per worktree; merge order = dependency order at pr-ready; the delegate brief carries settled contracts; a scope-change signal naming a sibling's active scope is refused).

Review for: (1) state-safety of the per-task record layout — can two worktrees still race on any shared file (the git-local stages.json, .factory/stages.json, the decomposition copies, roadmap.json)? does migration from the legacy single file lose fields or reorder stages? (2) the scope-disjointness check — prefixes vs named files, amendments, required-test files; can two stages both be active on overlapping paths through any path (task start vs stage start vs reopen)? (3) dependency gating — can a task start before a dependency's marker is on the trunk, or pr-ready before its dependencies shipped? (4) `active_stages_everywhere` scanning linked worktrees — cost and failure when a worktree is gone/prunable; (5) any behaviour change for the single-task (sequential) flow that existing clients rely on. P0–P2 only, file:line.
