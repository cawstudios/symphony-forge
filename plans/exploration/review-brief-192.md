# Review brief — symphony-forge #192 seal measures, bounded coordinator fixes, plan review records

Scope: feat/seal-measures-and-plan-review vs main (after #189). Owner decisions implemented: (1) stage done refuses only on failing verify/tests or a blocking review; scope strays, budget overruns and required-test id misses are measured and recorded on the stage (`measured`), printed as notes, shown by stage list; only >2x the line budget refuses; required tests match by exact id or id-prefix; decision records never count as strays. (2) A CLOSED degraded window with <=5 files all inside the task's effective write scope satisfies `_require_successful_launch` as the "who wrote" proof (recorded as `host_window`). (3) The plan digest hashes the body without frontmatter (so `plan save`'s `saved:` timestamp never invalidates a grill/approval); the grounding digest excludes harness-owned paths from the pre-stage product tree; plan/task approve no longer gate on the board-view marker. (4) Caps: outcome 800->2000, objective 500->2000; check_pr_ticket counts only the PR's own first-parent commits. (5) doctor --fix refreshes the autoreview skill from upstream when a recorded upstream sha differs.

Review for: (a) does any measured-not-refused path let a task seal with product changes outside every declared area AND no record of it (the record must be complete and visible), (b) can the host-window proof be satisfied by a window that touched files outside scope or that is still open, or by a window from another task/story, (c) does the body-only plan digest let a frontmatter change that matters (decisions_reviewed list) escape re-grilling, and do all callers use the same digest (grill record, approve, decomposition binding, task grill), (d) the grounding exclusion list — anything product-like excluded by accident, (e) required-test prefix matching false positives (a short id matching a different test), (f) doctor: the ls-remote/network path must never fail the doctor run offline and must not overwrite a user-modified skill silently, (g) check_pr_ticket first-parent logic vs merge commits on the PR branch. P0-P2 only, file:line.
